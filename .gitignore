# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/

# Data: keep raw Kaggle competition CSVs out of git; commit our derived/processed dataset.
data/raw/*
!data/raw/.gitkeep

# Model artifacts
adapters/
merged/
outputs/
*.gguf
wandb/

# Local prediction / eval run artifacts (regenerated by the notebook + frontier runner; not committed).
# Anchored to repo root so committed datasets under data/processed/ are untouched.
/predictions_*.jsonl
/*_smoke.jsonl

# OS
.DS_Store
