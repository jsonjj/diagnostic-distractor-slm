{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wayline.game/contracts/v1/session-create.schema.json",
  "title": "Wayline Session Create",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "requestId", "profileId", "clientBuild"],
  "properties": {
    "schemaVersion": {"const": "wayline.v1"},
    "requestId": {
      "type": "string",
      "minLength": 3,
      "maxLength": 96,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{2,95}$"
    },
    "profileId": {
      "type": "string",
      "minLength": 3,
      "maxLength": 96,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{2,95}$"
    },
    "clientBuild": {
      "type": "string",
      "minLength": 3,
      "maxLength": 96,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{2,95}$"
    }
  }
}
